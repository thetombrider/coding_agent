Find files by glob pattern (e.g. "**/*.ts", "src/**/*.test.ts").

Use for structure discovery (which files exist). To search file contents, use grep instead.
Avoid "src/**/*" on large repos — scope to a subdirectory or use a specific extension pattern.
