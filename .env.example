# Optional API keys — override ~/.orin/config.json when set.
# Get an OpenAI Platform key at https://platform.openai.com/api-keys
# then run `/providers configure openai` in the TUI (or set here).
OPENAI_API_KEY=
